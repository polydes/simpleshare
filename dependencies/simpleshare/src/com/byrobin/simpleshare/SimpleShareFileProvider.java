package com.byrobin.simpleshare;

/**
 * Let the &lt;provider&gt; entry's android:name attribute have a unique
 * name that doesn't clash with other extensions
 */
public class SimpleShareFileProvider extends androidx.core.content.FileProvider
{
}